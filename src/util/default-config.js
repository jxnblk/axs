
import defaultConfig from 'understyle'

export { defaultConfig as default } from 'understyle'

export const {
  breakpoints,
  scale,
  typeScale,
  colors,
  borderRadius,
  bold,
} = defaultConfig

